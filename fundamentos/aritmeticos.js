const [a, b, c, d] = [10, 5, 40, 8]

const soma = a + b + c + d
const subtracao = d - a
const multiplicacao = a * c
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)